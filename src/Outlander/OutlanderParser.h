#import <PEGKit/PKParser.h>

enum {
    OUTLANDERPARSER_TOKEN_KIND_OPEN_CURLY = 14,
    OUTLANDERPARSER_TOKEN_KIND_GE,
    OUTLANDERPARSER_TOKEN_KIND_DOUBLE_AMPERSAND,
    OUTLANDERPARSER_TOKEN_KIND_FOR,
    OUTLANDERPARSER_TOKEN_KIND_BREAK,
    OUTLANDERPARSER_TOKEN_KIND_CLOSE_CURLY,
    OUTLANDERPARSER_TOKEN_KIND_RETURN,
    OUTLANDERPARSER_TOKEN_KIND_GOTO,
    OUTLANDERPARSER_TOKEN_KIND_PLUS_EQUALS,
    OUTLANDERPARSER_TOKEN_KIND_FUNCTION,
    OUTLANDERPARSER_TOKEN_KIND_IF,
    OUTLANDERPARSER_TOKEN_KIND_NEW,
    OUTLANDERPARSER_TOKEN_KIND_ELSE,
    OUTLANDERPARSER_TOKEN_KIND_BANG,
    OUTLANDERPARSER_TOKEN_KIND_FINALLY,
    OUTLANDERPARSER_TOKEN_KIND_COLON,
    OUTLANDERPARSER_TOKEN_KIND_CATCH,
    OUTLANDERPARSER_TOKEN_KIND_PAUSE,
    OUTLANDERPARSER_TOKEN_KIND_SEMI_COLON,
    OUTLANDERPARSER_TOKEN_KIND_DO,
    OUTLANDERPARSER_TOKEN_KIND_LT,
    OUTLANDERPARSER_TOKEN_KIND_MINUS_EQUALS,
    OUTLANDERPARSER_TOKEN_KIND_PERCENT,
    OUTLANDERPARSER_TOKEN_KIND_EQUALS,
    OUTLANDERPARSER_TOKEN_KIND_THROW,
    OUTLANDERPARSER_TOKEN_KIND_TRY,
    OUTLANDERPARSER_TOKEN_KIND_GT,
    OUTLANDERPARSER_TOKEN_KIND_REGEXBODY,
    OUTLANDERPARSER_TOKEN_KIND_TYPEOF,
    OUTLANDERPARSER_TOKEN_KIND_OPEN_PAREN,
    OUTLANDERPARSER_TOKEN_KIND_WHILE,
    OUTLANDERPARSER_TOKEN_KIND_VAR,
    OUTLANDERPARSER_TOKEN_KIND_CLOSE_PAREN,
    OUTLANDERPARSER_TOKEN_KIND_DOUBLE_PIPE,
    OUTLANDERPARSER_TOKEN_KIND_STAR,
    OUTLANDERPARSER_TOKEN_KIND_PLUS,
    OUTLANDERPARSER_TOKEN_KIND_OPEN_BRACKET,
    OUTLANDERPARSER_TOKEN_KIND_COMMA,
    OUTLANDERPARSER_TOKEN_KIND_DELETE,
    OUTLANDERPARSER_TOKEN_KIND_SWITCH,
    OUTLANDERPARSER_TOKEN_KIND_MINUS,
    OUTLANDERPARSER_TOKEN_KIND_IN,
    OUTLANDERPARSER_TOKEN_KIND_CLOSE_BRACKET,
    OUTLANDERPARSER_TOKEN_KIND_DOT,
    OUTLANDERPARSER_TOKEN_KIND_DEFAULT,
    OUTLANDERPARSER_TOKEN_KIND_SETVARIABLE,
    OUTLANDERPARSER_TOKEN_KIND_FORWARD_SLASH,
    OUTLANDERPARSER_TOKEN_KIND_CASE,
    OUTLANDERPARSER_TOKEN_KIND_LE,
    OUTLANDERPARSER_TOKEN_KIND_PUT,
};

@interface OutlanderParser : PKParser

@end

